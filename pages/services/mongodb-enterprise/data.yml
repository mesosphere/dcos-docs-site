# Common values:

packageName: MongoDB
serviceName: MongoDB
techName: MongoDB
techShortName: MongoDB
minNodeCount: three
# Values specific to certain templates:

operations:
  default-deploy: |
    deploy (serial strategy) (COMPLETE)
    ├─ mongodb (serial strategy) (COMPLETE)
    │  └─ mongod:[server] (COMPLETE)
