---
layout: layout.pug
navigationTitle:  dcos monitoring prometheus status config
title: dcos monitoring prometheus status config
menuWeight: 1
excerpt: Return the currently loaded configuration file
---

# Description

The `dcos monitoring prometheus status config` returns the loaded configuration file.

# Usage

```bash
dcos monitoring prometheus status config [flags]
```

# Options

| Name |  Description |
|---------|-------------|
| `--help, h`   |   Display usage. |

# Parent command

| Command | Description |
|---------|-------------|
| [`dcos monitoring prometheus status`](../) |Expose current Prometheus configuration |
